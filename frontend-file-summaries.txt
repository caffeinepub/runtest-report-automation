{
  "frontend/src/index.css": "Global styles with dark navy OKLCH color system, amber/orange accent tokens, custom Inter/JetBrains Mono fonts, scrollbar styling, and utility classes for data tables and navigation.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono fonts, navy and amber custom color palettes, amber glow shadows, and fade-in animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration — updated `useUpsertReport` and `useDirectUpsert` to accept and pass an optional `location` string field (10th parameter) to the backend `upsertReport` and `upsertBatchReport` calls; all other hooks and helpers unchanged.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header (logo + nav links), main content area, and footer with caffeine.ai attribution and dynamic year.",
  "frontend/src/components/WeekNavigator.tsx": "Week navigation component with prev/next buttons — now self-contained with its own `parseWeekLabel` and `getAdjacentWeek` helpers (no longer importing from `useQueries`), exported as both named and default.",
  "frontend/src/components/ModelSummaryCard.tsx": "Model summary card extended with two new breakdown sections — \"By Flavour\" and \"By Location\" — each showing a mini bar chart of distinct unit counts per group, using `countDistinctUnits` for consistent counting with the Reports table.",
  "frontend/src/components/DataEntryForm.tsx": "Manual bulk data entry form — fixes the `FLAVOUR_LABELS` record to include all four `Flavour` enum values (`standard`→Lite, `aqi`→AQI, `premium`→Premium, `deluxe`→Deluxe) and updates `ALL_FLAVOURS` to expose all four options in the dropdown.",
  "frontend/src/components/WeeklyReportTable.tsx": "Report table with sortable columns — fixes the `flavourLabel` function to correctly map backend enum values (`aqi`→`AQI`, `premium`→`Premium`, `standard`→`Lite`, `deluxe`→`Deluxe`) instead of naive capitalization, ensuring AQI displays as \"AQI\" not \"Aqi\"; all other table logic (sorting, totals, distinct unit count footer) unchanged.",
  "frontend/src/components/ReportFilters.tsx": "Filter bar with CSV export updated to include the new \"Location\" column (alongside existing Model and Flavour), with proper label formatting and comma-escaping for all string fields.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page unchanged in structure but now explicitly documents that `filterValidEntries` is the single source of truth for valid report filtering, ensuring the week-scoped entries passed to `ModelSummaryCard` are consistent with what `ReportPage` shows.",
  "frontend/src/pages/DataEntryPage.tsx": "Data entry page updated to use the named `{ CSVImportSection }` import and `getISOWeekLabel` from `useQueries`, passing `selectedWeek` prop to `CSVImportSection`.",
  "frontend/src/pages/ReportPage.tsx": "Reports page that fetches all reports, derives available weeks, and wires `WeekNavigator` (using `currentWeek`/`onWeekChange`) and `ReportFilters` (using `availableWeeks`/`onFilterChange`) with their correct prop interfaces, then passes filtered entries to `WeeklyReportTable`.",
  "frontend/src/App.tsx": "Root app with TanStack Router — uses default import for DataEntryPage (matching its export style) alongside DashboardPage and ReportPage.",
  "frontend/src/utils/csvParser.ts": "CSV/XLS parser for Waggle Portal GPS tracker exports — fixes the \"Address not found\" unit ID bug by (1) expanding INVALID_UNIT_IDS to include the literal \"address not found\" string so it's recognized as invalid, (2) adding a `resolveUnitId()` helper that tries Address column → HWID column → filename extraction in order, (3) adding `findHwidColumnIndex()` as an alternative ID source, (4) improving `extractUnitIdFromFilename()` to correctly parse Waggle Portal filenames like `S18025_PRD.xls`, and (5) adding `resolvedSampleRecords` to `ParseDebugInfo` so the debug panel can show actual aggregated numeric counts per unit instead of \"aggregated\" placeholders.",
  "frontend/src/components/CSVImportSection.tsx": "Multi-file CSV/XLS import component with Model/Flavour/Location selectors — fixes the `ColumnMappingSelector` call by passing the required `availableColumns={[]}` prop, fixes AQI flavour mapping to use `Flavour.aqi`, and deduplicates records by `unitId+weekYear` before batch upload.",
  "frontend/index.html": "HTML entry point with SheetJS full build loaded via CDN script tag to ensure XLS/XLSX support is available globally before any parsing code runs.",
  "frontend/src/utils/reportFilters.ts": "Utility module exporting `INVALID_UNIT_IDS`, `isValidUnitId`, `filterValidEntries`, and a new `countDistinctUnits` helper that counts unique unit IDs — used consistently across Dashboard and Reports to eliminate count mismatches.",
  "frontend/src/hooks/useColumnMapping.ts": "New custom hook that manages user-selected custom CSV column names with localStorage persistence (key: 'gps-tracker-custom-columns'). Provides toggleColumn, selectAll, clearAll, isSelected, and getColumnValue helpers.",
  "frontend/src/components/ColumnMappingSelector.tsx": "New component that renders all available CSV column headers as grouped checkboxes (GPS & Location, Device Info, Environmental, Network, Other), with Select All / Clear All buttons, using the useColumnMapping hook for state."
}
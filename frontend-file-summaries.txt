{
  "frontend/src/index.css": "Global styles with dark navy OKLCH color system, amber/orange accent tokens, custom Inter/JetBrains Mono fonts, scrollbar styling, and utility classes for data tables and navigation.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono fonts, navy and amber custom color palettes, amber glow shadows, and fade-in animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration — updated `useUpsertReport` and `useDirectUpsert` to accept and pass an optional `storedPkts` bigint parameter (7th backend arg = `storedPktCount`) and `normalPkts` bigint parameter (8th backend arg), both defaulting to `BigInt(0)` when not provided, matching the full 8-parameter `upsertReport` backend signature.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header (logo + nav links), main content area, and footer with caffeine.ai attribution and dynamic year.",
  "frontend/src/components/WeekNavigator.tsx": "Week navigation component with prev/next buttons, now exported as both named export and default export to support both import styles.",
  "frontend/src/components/ModelSummaryCard.tsx": "Summary card for a single GPS unit model that now counts distinct `unitId` values (via `Set`) from the pre-filtered, week-scoped `entries` prop, ensuring the unit badge matches the report page's per-model unit count.",
  "frontend/src/components/DataEntryForm.tsx": "Bulk data entry form updated to pass both `storedPkts` (same value as `stored`, for the dedicated `storedPktCount` backend field) and `normalPkts` in the `mutateAsync` call, matching the full 8-parameter backend signature.",
  "frontend/src/components/WeeklyReportTable.tsx": "Report table updated to import useColumnMapping and dynamically render additional header/body/footer cells for each user-selected custom column, with amber-tinted styling to distinguish them from built-in columns.",
  "frontend/src/components/ReportFilters.tsx": "Filter bar with CSV export updated to include a \"Normal Packets\" column header and per-row/totals normal packet count values in the exported file.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard overview page importing `ModelSummaryCard` and `WeekNavigator` as named exports and `getISOWeekLabel` from `useQueries`, with KPI cards, per-model breakdown, and custom columns notice.",
  "frontend/src/pages/DataEntryPage.tsx": "Data entry page with file import and manual entry tabs, week navigator, and info card about custom column mapping — imports `DataEntryForm` as named export, `WeekNavigator` as named export, and `getISOWeekLabel` from `useQueries`.",
  "frontend/src/pages/ReportPage.tsx": "Weekly report page that correctly passes all required props to `ReportFilters` (selectedModel, onModelChange, selectedWeek, onWeekChange, availableWeeks, filteredEntries, modelWeekEntries, selectedUnitId, onUnitIdChange), with custom columns badge list and WeeklyReportTable.",
  "frontend/src/App.tsx": "Root app with TanStack Router — uses default import for DataEntryPage (matching its export style) alongside DashboardPage and ReportPage.",
  "frontend/src/utils/csvParser.ts": "CSV/XLS parser for Waggle Portal GPS tracker exports — fixes the \"Address not found\" unit ID bug by (1) expanding INVALID_UNIT_IDS to include the literal \"address not found\" string so it's recognized as invalid, (2) adding a `resolveUnitId()` helper that tries Address column → HWID column → filename extraction in order, (3) adding `findHwidColumnIndex()` as an alternative ID source, (4) improving `extractUnitIdFromFilename()` to correctly parse Waggle Portal filenames like `S18025_PRD.xls`, and (5) adding `resolvedSampleRecords` to `ParseDebugInfo` so the debug panel can show actual aggregated numeric counts per unit instead of \"aggregated\" placeholders.",
  "frontend/src/components/CSVImportSection.tsx": "File import component with drag-and-drop, debug panel, and custom column mapping UI — updated debug panel to display `resolvedSampleRecords` (actual aggregated unit IDs and numeric packet counts) instead of raw sample row lookups that showed \"Address not found\", and simplified the import filter to trust the parser's already-validated records.",
  "frontend/index.html": "HTML entry point with SheetJS full build loaded via CDN script tag to ensure XLS/XLSX support is available globally before any parsing code runs.",
  "frontend/src/utils/reportFilters.ts": "New utility module that exports `INVALID_UNIT_IDS` (a Set of known bad unit ID strings), `isValidUnitId` (checks a single ID against the set and the leading-dash rule), and `filterValidEntries` (filters a `ReportEntry[]` to remove all invalid unit IDs).",
  "frontend/src/hooks/useColumnMapping.ts": "New custom hook that manages user-selected custom CSV column names with localStorage persistence (key: 'gps-tracker-custom-columns'). Provides toggleColumn, selectAll, clearAll, isSelected, and getColumnValue helpers.",
  "frontend/src/components/ColumnMappingSelector.tsx": "New component that renders all available CSV column headers as grouped checkboxes (GPS & Location, Device Info, Environmental, Network, Other), with Select All / Clear All buttons, using the useColumnMapping hook for state."
}
{
  "frontend/src/index.css": "Global styles with dark navy OKLCH color system, amber/orange accent tokens, custom Inter/JetBrains Mono fonts, scrollbar styling, and utility classes for data tables and navigation.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono fonts, navy and amber custom color palettes, amber glow shadows, and fade-in animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration — fixed all mutation functions (`useUpsertReport`, `useDirectUpsert`, and legacy aliases `useCreateReport`/`useUpdateReport`) to call `actor.upsertReport` instead of the non-existent `actor.createReport`/`actor.updateReport`, resolving the root cause of all import failures.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header (logo + nav links), main content area, and footer with caffeine.ai attribution and dynamic year.",
  "frontend/src/components/WeekNavigator.tsx": "Week navigation component with prev/next buttons, now exported as both named export and default export to support both import styles.",
  "frontend/src/components/ModelSummaryCard.tsx": "Summary card for a single GPS unit model showing unit count, total/stored/valid GPS packet counts, and animated progress bars for GPS fix and stored rates.",
  "frontend/src/components/DataEntryForm.tsx": "Bulk data entry form with model/week selectors, dynamic row table for up to 30 units, per-row validation, quick-add buttons, and upsert submission with toast feedback.",
  "frontend/src/components/WeeklyReportTable.tsx": "Report table that receives pre-filtered entries from the parent, sorts them by unit ID, and displays per-unit packet data with color-coded GPS fix badges and a totals footer row.",
  "frontend/src/components/ReportFilters.tsx": "Filter bar with week navigator, model dropdown, unit ID dropdown (populated from model+week filtered entries with \"All Units\" default), and CSV export button that respects all active filters including unit ID.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard overview page passing correct `entries` prop (not `reports`/`modelLabel`) to `ModelSummaryCard`, with auto-selection of the most recent available week when imported data arrives.",
  "frontend/src/pages/DataEntryPage.tsx": "Data entry page using default import for `CSVImportSection` to match its export style.",
  "frontend/src/pages/ReportPage.tsx": "Weekly report page that now applies `filterValidEntries` to the raw backend data immediately after fetching, ensuring all downstream consumers (table, filters, CSV export, unit ID dropdown, and totals) only ever see valid unit IDs like `S10002-PRD`.",
  "frontend/src/App.tsx": "Root app with TanStack Router setup using correct default imports for `DashboardPage` and `ReportPage`, and named import for `DataEntryPage`.",
  "frontend/src/utils/csvParser.ts": "Waggle portal CSV/XLS/XLSX parser where the unit ID is exclusively derived from the filename stem — no column value or row content is ever used as a unit ID. All rows are set to `unitId: 'pending'` during parsing and then replaced with the filename stem in `aggregateToSingleUnit`, ensuring values like 'Charging', 'Fair', 'Good', 'Poor', 'unknown' can never leak into the unit ID.",
  "frontend/src/components/CSVImportSection.tsx": "File import component that derives unit ID exclusively from the filename stem; updated error toast to include the specific backend error message so users see the root cause when an upsert fails, with detailed console logging for each upsert attempt.",
  "frontend/index.html": "HTML entry point with SheetJS full build loaded via CDN script tag to ensure XLS/XLSX support is available globally before any parsing code runs.",
  "frontend/src/utils/reportFilters.ts": "New utility module that exports `INVALID_UNIT_IDS` (a Set of known bad unit ID strings), `isValidUnitId` (checks a single ID against the set and the leading-dash rule), and `filterValidEntries` (filters a `ReportEntry[]` to remove all invalid unit IDs)."
}